Scalar (값) -> 변수 -> Data type 

Data type 의 종류
숫자 )
문자 ) 기본
논리 )
--------------
NULL ) 값이 없다
NA    ) 결측 치 
NaN  ) 계산 불가
--------------
Factor ) 범주 형 (한번 팩터형 함수가 선언되면 그 안에있는 함수말고는 추가불가.

변수, 특성 = 열
관측치 = 행

- 1차원 배열(동일 자료형 집합) -> Vector(배열)
c( 함수 )

- 1차원 배열(다른 자료형 집합) -> List


- 2차원 배열(행/열 집합) -> 

Matrix ( 동일자료형 행/열 집합 ) ( 비어서 생성 불가 )
			***Data Frame (다른자료형 가능)


Data Frame (다른자료형 가능) 단, 하나의 열(변수)은 같은 데이터여야함. 행(관측치)는 무상관.

CSV 파일은 컴마로 구분
TSV 파일은 Tap 으로 구분

Text file -> Ascii 형식
Binary File -> 바이너리 형식
 
( api )or 제이슨 형식은 날씨