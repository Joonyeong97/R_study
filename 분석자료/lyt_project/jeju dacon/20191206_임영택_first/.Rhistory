saasd
require(VIM)
agg <- aggregate( iris[, -5] ,
by = list(iris$Species),
FUN = mean ) #데이터 집계 함수
agg
agg <- aggregate( iris[, -5] ,
by = list(iris$Species),
FUN = mean ) #데이터 집계 함수
agg
agg1 <- aggregate( iris[, -5] ,
by = list(iris$Species),
FUN = sd ) #데이터 집계 함수
agg1
agg3 <- aggregate( mtcars ,
by = list(cyl = mtcars$cyl,
vs = mtcars$vs),
FUN = max ) #데이터 집계 함수
agg3
jejufull <- read.csv('bus_bts12.csv', header = T)
jeju_mmdd <- jejufull %>% select('날짜')
library(dplyr)
jeju_mmdd <- jejufull %>% select('날짜')
jeju_mmdd
table(jeju_mmdd)
jeju_mmdd1 <- jeju_mmdd %>% select('2019-09-01')
jeju_mmdd1 <- jeju_mmdd %>% select(2019-09-01)
jeju_mmdd1 <- jeju_mmdd %>% filter(2019-09-01)
jeju_mmdd1 <- jeju_mmdd %>% filter('2019-09-01')
cor(mtcars      )
pairs(mtcars    )
par(mfrow = c(1,1))
month <- 1:12
late <- c(5,8,7,9,4,6,12,13,8,6,6,4)
plot(month, late, main = '지각생 통계',
type = 'l', lty = 1, lwd = 1, las = 1,
xlab = 'Month', ylab = 'late cnt')
